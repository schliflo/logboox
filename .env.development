# The public variables `vite dev` serves.
#
# In production these come from the Worker's `vars`; locally there is no Worker
# in front of the dev server, so they are set here. Public by definition —
# anything secret belongs in `.dev.vars`, which is not committed.
PUBLIC_SITE_URL=http://localhost:5173
PUBLIC_ACCOUNTS=1
